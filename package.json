{
  "name": "papeleria-backend",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "workspaces": [
    "core",
    "shared",
    "frontend"
  ],
  "scripts": {
    "build": "npm install --prefix core --omit=dev",
    "start": "node core/src/index.js"
  }
}
